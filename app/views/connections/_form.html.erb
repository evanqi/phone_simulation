<%= form_for(@connection) do |f| %>
  <% if @connection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@connection.errors.count, "error") %> prohibited this connection from being saved:</h2>

      <ul>
      <% @connection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :state %><br>
    <%= f.number_field :state %>
  </div>
  <div class="field">
    <%= f.label :incoming_url %><br>
    <%= f.text_field :incoming_url %>
  </div>
  <div class="field">
    <%= f.label :incoming_phone_num %><br>
    <%= f.text_field :incoming_phone_num %>
  </div>
  <div class="field">
    <%= f.label :incoming_caller_id %><br>
    <%= f.text_field :incoming_caller_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
